[order]
description = "Flatten Dolt commit history to reduce storage"
trigger = "cooldown"
interval = "24h"
exec = "GC_DOLT_COMPACT_REQUIRE_APPLICABLE=1 gc dolt compact 2>&1 | gc dolt health-check --compact-result"
timeout = "24h"
